# find("a") returns the index of the first "a" in the string.
# "Hello world!" contains no "a", so find returns -1.
# Prediction: -1
string = "Hello world!"
position = string.find("a")
print(position)
